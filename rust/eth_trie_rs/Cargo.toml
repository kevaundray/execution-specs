[package]
name = "eth_trie_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "eth_trie_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
# pinned exactly; consensus-critical (encoding must not shift)
alloy-trie = "=0.7.4"
# pinned exactly; consensus-critical (encoding must not shift)
alloy-primitives = "=0.8.5"
# pinned exactly; consensus-critical (encoding must not shift)
alloy-rlp = { version = "=0.3.8", features = ["derive"] }
