[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "eth_trie_rs"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "eth_trie_rs"
profile = "release"
